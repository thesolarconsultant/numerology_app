# NumeraFlow

Self-contained numerology web app — a single `index.html` with no backend and no build step.
Your family's numbers, decoded.

## Deploy
Static site. On Vercel: import this repo and deploy (no settings to change).
Everything is stored in the visitor's browser (localStorage); there is no server.

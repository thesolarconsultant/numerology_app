NUMIO app icons — cut from the wordmark's own N.

Upload the whole `icons` folder to the repo root of numerology_app. It replaces
the four files already there (three of which are still the old NumeraFlow star).

  icon-512.png            any-purpose, PWA
  icon-192.png            any-purpose, PWA
  icon-180.png            apple-touch-icon (iOS home screen)
  icon-maskable-512.png   Android adaptive — mark sits at 37% width, well inside
                          the 80% safe zone, so a round mask cannot clip it

ALTERNATIVE-plum-512.png is the inverse (cream N on plum) if you would rather
the icon shout than whisper. Say the word and I will cut the full set that way.
Do not upload that file itself — it is a proof, not an asset.
